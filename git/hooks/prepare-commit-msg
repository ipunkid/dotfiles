#!/bin/bash
COMMIT_FILE=$1
SOURCE=$2

if [ -n "$SOURCE" ]; then
    exit 0
fi

if [ -s "$COMMIT_FILE" ]; then
    exit 0
fi

AI_MSG=$(git-commit-ai 2>/dev/null)
if [ -n "$AI_MSG" ]; then
    echo "$AI_MSG" > "$COMMIT_FILE"
fi
